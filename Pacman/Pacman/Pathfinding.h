#pragma once

#include <queue>
#include <unordered_map>
#include <functional>

#include "PathmapTile.h"
class Pathfinding
{
public:
	Pathfinding(PathmapTile* tilemap);

	//std::vector<PathmapTile*> calculatePath(PathmapTile* start, PathmapTile* Goal);
private:
	//static inline float Heuristic(PathmapTile* a, PathmapTile* b);
	//PathmapTile* TileMap;


};

